# 初始状态
# initial state
# 初始位置, 纬度 经度 高程
# initial position, latitude, longitude, altitude. [deg, deg, m]
init_position_lla: [ 30.4604282594, 114.4725045425, 22.75837 ]

# 初始速度, 北向速度, 东向速度, 垂向速度
# initial velocity, speed in the directions of north, east and down. [m/s, m/s, m/s]
init_velocity: [ 0.00165, -0.00641, 0.003612 ]

# 初始姿态, 欧拉角(ZYX旋转顺序), 横滚, 俯仰, 航向
# initial attitude, euler angle(ZYX rotation), roll, pitch, yaw [deg, deg, deg]
init_attitude: [ 1.57299, 0.70472, 278.55380 ]

# 初始IMU零偏和比例因子, IMU的三个轴(前、右、下)
# initial IMU bias and scale error, three axes of the imu (forward, right and down)
init_gyro_bias: [ 0, 0, 0 ]    # [deg/h]
init_accel_bias: [ 0, 0, 0 ]    # [mGal]
init_gyro_scale: [ 0, 0, 0 ]   # [ppm]
init_accel_scale: [ 0, 0, 0 ]   # [ppm]

# kalman prediction process std
init_position_std: [0.005, 0.004, 0.008] # [m]
init_velocity_std: [ 0.003, 0.004, 0.004 ] # [m/s]
init_rotation_std: [ 0.003, 0.003, 0.023 ] # [deg]
init_bg_std: [ 20, 20, 20 ]          # [deg/h]
init_ba_std: [ 100, 100, 100 ]       # [mGal]
init_sg_std: [ 300, 300, 300 ]    # [ppm]
init_sa_std: [ 300, 300, 300 ]    # [ppm]

# imu sensor noise
arw: [0.2, 0.2, 0.2]          # [deg/sqrt(hr)]
vrw: [0.1, 0.1, 0.1]           # [m/s/sqrt(hr)]
bg_std: [20, 20, 20]        # [deg/hr]
ba_std: [100, 100, 100]     # [mGal]
sg_std: [300.0, 300.0, 300.0]  # [ppm]
sa_std: [300.0, 300.0, 300.0]  # [ppm]
corr_time: 1.0                    # [hr]

# kalman measurement process std (uint: m)
gps_position_x_std: 0.01
gps_position_y_std: 0.01
gps_position_z_std: 0.03

# 天线杆臂, IMU坐标系前右下方向
# antenna lever, forward, right and down in the imu frame. [m]
ant_lever:  [ 0.045, 0.46, -0.238 ]

# only use prediction, equivalent to only using IMU to integration
only_prediction: false

# consider earth rotation
is_earth_rotation: true

# duration 357473.0071-359089.9880
start_time: 357473 
end_time: 358000